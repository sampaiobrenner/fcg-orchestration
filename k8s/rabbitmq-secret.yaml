# Valores de desenvolvimento para cluster local (kind/minikube/Docker Desktop). Nao reutilize em ambientes reais.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secret
  labels:
    app: rabbitmq
type: Opaque
stringData:
  RABBITMQ_DEFAULT_USER: "fcg"
  RABBITMQ_DEFAULT_PASS: "fcg"
