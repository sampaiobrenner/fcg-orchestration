apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  labels:
    app: notifications-api
spec:
  type: ClusterIP
  selector:
    app: notifications-api
  ports:
    - name: http
      port: 80
      targetPort: http
