apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  labels:
    app: postgres
data:
  init.sql: |
    CREATE DATABASE fcg_users;
    CREATE DATABASE fcg_catalog;
    CREATE DATABASE fcg_payments;
    CREATE DATABASE fcg_notifications;
